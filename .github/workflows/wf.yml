on: [ workflow_dispatch ]

jobs:
  docker-job:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo $USER
        touch file.txt
        sudo touch sfile.txt
        sudo whoami
        ls -a
